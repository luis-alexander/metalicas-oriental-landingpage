---
//Datos para cada componente 
interface Props {
  title?: string;
  link?: string;
}

//Extraemos el dato title y content de la respuesta y si no hay, que muestre por defecto los valores 
const { title = "Título por defecto" , link = "#"} = Astro.props;
---

<a href={link}
  type="button" 
  class="bg-gradient-to-r from-red-500 to-red-600 text-white font-semibold px-5 py-2 rounded-2xl shadow-md hover:from-red-600 hover:to-red-700 transition-all duration-300 ease-in-out transition-all duration-500 hover:scale-105 hover:shadow-2xl hover:rotate-1"
>
  {title}
</a>


